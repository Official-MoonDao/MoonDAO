name: E2E Tests
on: [push]
jobs:
  cypress-run:
    runs-on: unbuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Cypress run
        uses: cypress-io/github-action@v5
